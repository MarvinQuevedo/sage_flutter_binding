# Run with `dart run ffigen --config ffigen.yaml`.
name: SageFlutterBindingBindings
description: |
  Bindings for `src/sage_flutter_binding.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/sage_flutter_binding_bindings_generated.dart'
headers:
  entry-points:
    - 'src/sage_flutter_binding.h'
  include-directives:
    - 'src/sage_flutter_binding.h'
comments:
  style: any
  length: full
