# @ozone/wallet-wasm

This package is **generated** — do not edit by hand.

It is produced by `wasm-pack build` from the `crates/sage-wasm` crate in the Sage fork (see `../../../vendor/sage/`).

To regenerate:

```bash
# From the workspace root:
pnpm wasm:build
pnpm wasm:opt    # optional optimization pass
```

The Rust source is on the same branch (`web/ozone-extension`) under `vendor/sage/crates/sage-wasm/`.
